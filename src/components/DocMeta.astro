---
interface Props {
  maturity: 'Verified' | 'Experimental' | 'Historical' | 'Admin';
  lastVerified?: string;
  audience?: string;
  products?: string[];
  requiresVpn?: boolean;
}

// Guide metadata remains available in content frontmatter but is intentionally
// not rendered as a block at the start of each page.
---
