---
const { head } = Astro.locals.starlightRoute;
const headWithoutIcons = head.filter(
  ({ attrs }) => attrs?.rel !== 'icon' && attrs?.rel !== 'shortcut icon'
);
---

{headWithoutIcons.map(({ tag: Tag, attrs, content }) => <Tag {...attrs} set:html={content} />)}
